import React from 'react';
import { Card } from 'react-bootstrap';

function Home() {
    return (
        <Card>
            <Card.Body>
                <Card.Title>IoT Web Control System</Card.Title>
                <Card.Text>React + Spring Boot + MySQL</Card.Text>
            </Card.Body>
        </Card>
    );
}

export default Home;
